主要思路分析：
1、由于所有的请求都需要通过spring来进行控制，所以建立一个dispatcherServlet，所有的请求处理都通过此方法进行转发
配置dispatcherServlet,在该方法中，进行springmvc容器的初始化工作。
1）获取到配置的配置文件
2）解析配置文件，加载bean对象并实例化
3）完成对象的属性填充
4）遍历对象，获取是否有标注requestMapping,完成方法和uri的映射关系